on:
  push:
    branches:
      - main
  pull_request:

jobs:
  deploy:
    uses: dfe-analytical-services/dfeshiny/.github/workflows/automated_tests_template.yaml@new-feature/automated-tests-template
